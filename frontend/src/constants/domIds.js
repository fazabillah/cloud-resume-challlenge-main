/**
 * Centralized DOM element IDs used throughout the application
 * This makes it easier to maintain and update element IDs in one place
 */
export const DOM_IDS = {
  // Top Navigation
  topNav: 'topNav',
  topNavLinks: 'topNavLinks',
  hamburger: 'hamburger',

  // Side Navigation
  sideNav: 'sideNav',
  navbarSupportedContent: 'navbarSupportedContent',

  // Page sections
  pageTop: 'page-top',
}

export default DOM_IDS
